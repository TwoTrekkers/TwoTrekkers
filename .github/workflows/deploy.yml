name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build with Nuxt
        run: npm run generate
        env:
          NODE_ENV: production
          NUXT_PUBLIC_SITE_URL: https://twotrekkers.github.io
          NUXT_PUBLIC_CDN_URL: https://twotrekkers.nyc3.cdn.digitaloceanspaces.com

      - name: Deploy to GitHub Pages (push to gh-pages)
        if: github.ref == 'refs/heads/main'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          echo "🚀 Starting deployment process..."

          # Navigate to the build output directory
          cd .output/public

          # Initialize git and configure identity
          git init
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # Add all files and commit
          git add .
          git commit -m "Deploy: $(date '+%Y-%m-%d %H:%M:%S')" || echo "ℹ️  No changes to commit."

          # Set remote using GITHUB_TOKEN over HTTPS and force push to gh-pages
          git remote add origin https://x-access-token:${GITHUB_TOKEN}@github.com/${REPO}.git
          git branch -M gh-pages
          git push -f origin gh-pages

          echo "✅ Deployment complete!"
          echo "🌐 Your site should be available at: https://twotrekkers.github.io"
